name: Bulletin Policy
description: Base policy that defines permissions for bulletin namespaces
resources:
- name: namespace
  relations:
  - name: collaborator
    types: 
    - actor
  permissions:
  - name: create_post
    expr: collaborator
